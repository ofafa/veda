import React from 'react';
import ReactDom from 'react-dom';

import Composition from "../components/composition/composition.js";

const app = document.getElementById('main');
ReactDom.render(<Composition/>, app);
